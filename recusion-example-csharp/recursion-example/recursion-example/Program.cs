using System;

namespace recursion_example
{
    class Program
    {
        static void Main(string[] args)
        {
            
           // var result = SumOfArray.Compute();
           // var result = Fibonacci.Compute(); 
           //Console.WriteLine(result);
          // Combinations.Compute();
          // Permutations.Compute();
          StringPeriod.Compute();
        }
    }
}